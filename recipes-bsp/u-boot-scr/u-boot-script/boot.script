setenv grub_efi 'EFI/BOOT/bootaa64.efi'
setenv boot_efi 'bootefi ${loadaddr} ${fdtcontroladdr}'

load usb 0 ${loadaddr} ${grub_efi} && run boot_efi
load mmc 1 ${loadaddr} ${grub_efi} && run boot_efi
load mmc 2 ${loadaddr} ${grub_efi} && run boot_efi

setenv script

boot
