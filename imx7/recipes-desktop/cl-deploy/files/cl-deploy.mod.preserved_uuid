function sign_image_part() {

[[ -b ${SRC} && -z ${PTUUID} ]] && eval $(blkid --output export ${SRC})

[[ -z ${PTUUID} ]] && return

cat << eof | fdisk ${DST}
x
i
0x${PTUUID}
r
w
eof
}

sign_image_part
