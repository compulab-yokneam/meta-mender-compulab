MACHINE=${MACHINE:-cl-som-imx7}
TOOLS=../sources/meta-mender-compulab/imx7/tools
EULA=1

. setup-environment nxp

declare -a ENVIRONMENT=("${TOOLS}/setup-compulab-env")

for env in ${ENVIRONMENT[@]};do
[ -f ${env} ] && FORCE=Yes source ${env}
done
